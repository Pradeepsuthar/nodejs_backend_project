let express,APP_PORT,DB_URL,errorHandler,router,mongoose,path,cors;_ddf‍.w("express",[["default",["express"],function(v){express=v}]]);_ddf‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}],["DB_URL",["DB_URL"],function(v){DB_URL=v}]]);_ddf‍.w("./middlewares/errorHandler",[["default",["errorHandler"],function(v){errorHandler=v}]]);_ddf‍.w("./routes",[["default",["router"],function(v){router=v}]]);_ddf‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_ddf‍.w("path",[["default",["path"],function(v){path=v}]]);_ddf‍.w("cors",[["default",["cors"],function(v){cors=v}]]);

const app = express();






// Database connection
mongoose.connect(DB_URL, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
    useFindAndModify: false,
});

const db = mongoose.connection;
db.on('error', _ddf‍.g.console.error.bind(_ddf‍.g.console, 'connection error:'));
db.once('open', () => {
    console.log('DB connected...');
});

global.appRoot = path.resolve(__dirname);
app.use(cors());
app.use(express.urlencoded({ extended: false }));
app.use(express.json());


app.use('/api', router);
// Save media files here
app.use('/uploads', express.static('uploads'));

// Welcome page for api
app.use('/', (req, res) => {
    res.send(`
  <h1>Welcome to E-Commerce Rest APIs</h1>
  `);
});


// Middleware for error handling
app.use(errorHandler);

// Server initialization
const PORT = process.env.PORT || APP_PORT;
app.listen(PORT, () => {
    console.log(`Server is running on port ${PORT}`);
})