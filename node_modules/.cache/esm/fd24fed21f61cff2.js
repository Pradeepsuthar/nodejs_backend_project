let mongoose,APP_URL;_1f6‍.x([["default",()=>_1f6‍.o]]);_1f6‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_1f6‍.w("../config",[["APP_URL",["APP_URL"],function(v){APP_URL=v}]]);
const Schema = mongoose.Schema;


const productSchema = new Schema({
    title: { type: String, required: true },
    subTitle: { type: String, required: true },
    mrpPrice: { type: Number, default: 0 },
    salePrice: { type: Number, default: 0 },
    offer: { type: Number, default: 0 },
    isAvailable: { type: Boolean, default: false },
    thumbnail: {
        type: String,
        required: true,
        get: (thumbnail) => {
            // http://localhost:5000/uploads/products/1616443169266-52350494.png
            if (process.env.ON_HEROKU == 'true') {
                return `${thumbnail}`;
            }
            return `${APP_URL}/${thumbnail}`;
        },
    },
}, { timestamps: true, toJSON: { getters: true }, id: false });

_1f6‍.d(mongoose.model('Product', productSchema, 'products'));