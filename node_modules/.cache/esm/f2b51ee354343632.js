let User,RefreshToken,CustomErrorHandler,JwtService,bcrypt,REFRESH_SECRET,loginSchema,refreshSchema;_543‍.x([["default",()=>_543‍.o]]);_543‍.w("../../models",[["User",["User"],function(v){User=v}],["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}]]);_543‍.w("../../services",[["CustomErrorHandler",["CustomErrorHandler"],function(v){CustomErrorHandler=v}],["JwtService",["JwtService"],function(v){JwtService=v}]]);_543‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_543‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);_543‍.w("../../validators",[["loginSchema",["loginSchema"],function(v){loginSchema=v}],["refreshSchema",["refreshSchema"],function(v){refreshSchema=v}]]);





const loginController = {

    // Login User
    async login(req, res, next) {

        // Validation
        const { error } = loginSchema.validate(req.body);

        if (error) {
            return next(error);
        }

        try {
            const user = await User.findOne({ email: req.body.email });
            if (!user) {
                return next(CustomErrorHandler.wrongCredentials());
            }

            // compare the password
            const match = await bcrypt.compare(req.body.password, user.password);
            if (!match) {
                return next(CustomErrorHandler.wrongCredentials());
            }

            // Toekn
            const access_token = JwtService.sign({ _id: user._id, role: user.role });
            const refresh_token = JwtService.sign({ _id: user._id, role: user.role }, '1y', REFRESH_SECRET);

            // database whitelist
            await RefreshToken.create({ token: refresh_token });
            res.json({ access_token, refresh_token });

        } catch (err) {
            return next(err);
        }

    },

    // Logout User
    async logout(req, res, next) {

        // validation
        const { error } = refreshSchema.validate(req.body);

        if (error) {
            return next(error);
        }

        try {
            await RefreshToken.deleteOne({ token: req.body.refresh_token });
        } catch (err) {
            return next(new Error('Something went wrong in the database'));
        }

        res.json({ message: "User logout successfully!" });

    }
};


_543‍.d(loginController);